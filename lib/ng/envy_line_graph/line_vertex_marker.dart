import 'line_vertex_marker_tooltip.dart';

/// Defines a marker to display at a vertex.
class LineVertexMarker {
  /// Constructs a instance.
  LineVertexMarker(this.tooltip);

  /// Type of Shape.

  /// The optional tooltip to display when hovering over a vertex marker.
  final LineVertexMarkerTooltip tooltip;
}
